# CONTRIBUTING.md - Как внести вклад

Этот документ описывает, как вы можете внести свой вклад в развитие LakOS.

## 🤝 Как начать

### 1. Изучите проект

Прежде чем начать:
- Ознакомьтесь с [README.md](README.md)
- Прочитайте [ARCHITECTURE.md](ARCHITECTURE.md)
- Попробуйте собрать и запустить систему ([BUILDING.md](BUILDING.md))

### 2. Выберите задачу

#### Для новичков
- Исправление опечаток в документации
- Улучшение комментариев в коде
- Написание unit-тестов
- Улучшение Makefile

#### Для опытных разработчиков
- Реализация новых драйверов
- Добавление системных вызовов
- Оптимизация ядра
- Реализация многозадачности

### 3. Настройте окружение

Следуйте инструкциям из [BUILDING.md](BUILDING.md) для настройки среды разработки.

## 📝 Правила внесения изменений

### Стиль кода

#### C код
```c
// Используйте snake_case для функций и переменных
void my_function_name(void);

// Используйте PascalCase для структур
typedef struct {
    int field_name;
} MyStruct;

// Макросы - UPPER_CASE
#define MAX_SIZE 256

// Отступы - 4 пробела
if (condition) {
    do_something();
}

// Комментарии к функциям
/**
 * @brief Краткое описание функции
 * @param param1 Описание первого параметра
 * @param param2 Описание второго параметра
 * @return Описание возвращаемого значения
 */
int my_function(int param1, char* param2);
```

#### Ассемблер
```asm
; Используйте понятные метки
section .text
global my_function

; Комментируйте сложные операции
my_function:
    push ebp
    mov ebp, esp
    
    ; Загружаем первый аргумент
    mov eax, [ebp + 8]
    
    leave
    ret
```

### Безопасность

#### Проверка входных данных
```c
// Всегда проверяйте входные данные
void safe_function(char* input, size_t max_len) {
    if (!input || max_len == 0) {
        return;
    }
    
    // Проверяйте границы
    if (strlen(input) >= max_len) {
        return;
    }
    
    // Работайте с данными
    process_data(input);
}
```

#### Защита памяти
```c
// Проверяйте адреса памяти
int is_safe_address(void* addr) {
    if (addr == NULL) {
        return 0;
    }
    
    uint32_t address = (uint32_t)addr;
    if (address < 0x100000 || address > 0x1000000) {
        return 0;
    }
    
    return 1;
}
```

### Тестирование

#### Unit-тесты
```c
// tests/test_my_function.c
#include "include/my_function.h"
#include <assert.h>

void test_my_function(void) {
    // Тест 1: Нормальный случай
    int result = my_function(5, "test");
    assert(result == expected_value);
    
    // Тест 2: Граничные значения
    result = my_function(0, "");
    assert(result == expected_value);
    
    // Тест 3: Некорректные данные
    result = my_function(-1, NULL);
    assert(result == error_code);
}

void run_all_tests(void) {
    test_my_function();
    printf("All tests passed!\n");
}
```

#### Интеграционные тесты
```bash
#!/bin/bash
# tests/integration_test.sh

echo "Running integration tests..."

# Тест загрузки
qemu-system-i386 -cdrom lakos.iso -m 512M -nographic -serial stdio << EOF
echo "Test 1: System boot"
help
echo "Test 2: File operations"
ls
echo "Test 3: User operations"
whoami
shutdown
EOF

if [ $? -eq 0 ]; then
    echo "Integration tests passed!"
else
    echo "Integration tests failed!"
    exit 1
fi
```

## 🔄 Процесс внесения изменений

### 1. Форк и клонирование

```bash
# Сделайте форк репозитория на GitHub
# Склонируйте свой форк
git clone https://github.com/yourusername/lakos-os.git
cd lakos-os

# Добавьте upstream
git remote add upstream https://github.com/original/lakos-os.git
```

### 2. Создание ветки

```bash
# Создайте новую ветку для вашей задачи
git checkout -b feature/my-new-feature
# или
git checkout -b bugfix/fix-issue-123
```

### 3. Внесение изменений

```bash
# Вносите изменения
# Тестируйте изменения
make test

# Добавляйте файлы
git add .
git add tests/my_test.c

# Делайте коммит
git commit -m "Add my new feature

- Implement new functionality
- Add unit tests
- Update documentation"
```

### 4. Синхронизация с upstream

```bash
# Перед отправкой обновите свою ветку
git fetch upstream
git rebase upstream/main

# Или, если вы предпочитаете merge
git merge upstream/main
```

### 5. Отправка изменений

```bash
# Отправьте изменения в свой форк
git push origin feature/my-new-feature
```

### 6. Создание Pull Request

1. Перейдите на GitHub
2. Нажмите "New pull request"
3. Выберите свою ветку
4. Заполните шаблон PR

## 📋 Шаблон Pull Request

```markdown
## Описание изменений

Краткое описание того, что было изменено.

## Тип изменений

- [ ] Новая функция (не нарушает обратную совместимость)
- [ ] Исправление бага (не нарушает обратную совместимость)
- [ ] Назад несовместимое изменение (требует обновления документации)
- [ ] Исправление документации

## Тестирование

- [ ] Проходят ли unit-тесты?
- [ ] Проходят ли интеграционные тесты?
- [ ] Тестируется ли на реальном железе?

## Проверка кода

- [ ] Соответствует ли стиль кода?
- [ ] Есть ли комментарии к сложным участкам кода?
- [ ] Проверена ли безопасность?

## Документация

- [ ] Обновлена ли документация?
- [ ] Добавлены ли примеры использования?
- [ ] Обновлены ли CHANGELOG.md?

## Пример использования

```c
// Пример использования новой функции
int result = my_new_function(arg1, arg2);
```

## Дополнительная информация

Любая дополнительная информация, которая может быть полезна.
```

## 🐛 Сообщение о багах

### Как сообщить о баге

1. Проверьте, нет ли уже открытого issue
2. Создайте новое issue с подробным описанием
3. Приложите минимальный пример для воспроизведения

### Шаблон issue

```markdown
**Описание бага**
Краткое описание проблемы.

**Шаги для воспроизведения**
1. Запустите LakOS
2. Выполните команду `...`
3. Наблюдайте за поведением

**Ожидаемое поведение**
Описание того, что должно происходить.

**Фактическое поведение**
Описание того, что происходит на самом деле.

**Окружение**
- ОС: [например, Ubuntu 20.04]
- Версия: [например, 1.0.0]
- Компилятор: [например, GCC 9.3.0]

**Логи**
Приложите логи, если есть.

**Дополнительная информация**
Любая дополнительная информация о проблеме.
```

## ✨ Предложение улучшений

### Как предложить улучшение

1. Создайте issue с меткой `enhancement`
2. Опишите предлагаемое улучшение
3. Обоснуйте необходимость изменения

### Шаблон предложения

```markdown
**Суть предложения**
Краткое описание предлагаемого улучшения.

**Мотивация**
Почему это улучшение необходимо?

**Описание решения**
Как вы видите реализацию этого улучшения?

**Альтернативы**
Есть ли другие способы решения этой проблемы?

**Дополнительная информация**
Любая дополнительная информация.
```

## 📚 Ресурсы

### Для разработчиков
- [DEVELOPER_GUIDE.md](DEVELOPER_GUIDE.md) - Руководство разработчика
- [BUILDING.md](BUILDING.md) - Сборка системы
- [ARCHITECTURE.md](ARCHITECTURE.md) - Архитектура

### Инструменты
- [GitHub Issues](https://github.com/yourusername/lakos-os/issues)
- [GitHub Discussions](https://github.com/yourusername/lakos-os/discussions)
- [Code of Conduct](CODE_OF_CONDUCT.md)

### Образовательные ресурсы
- [OSDev Wiki](https://wiki.osdev.org/Main_Page)
- [x86 Assembly Guide](https://www.cs.virginia.edu/~evans/cs216/guides/x86.html)
- [GRUB Manual](https://www.gnu.org/software/grub/manual/)

## 🤔 Часто задаваемые вопросы

### Q: Как начать разработку под LakOS?
A: Начните с чтения [DEVELOPER_GUIDE.md](DEVELOPER_GUIDE.md) и настройки среды разработки.

### Q: Какие языки программирования используются?
A: Основной язык - C, для низкоуровневых операций используется ассемблер x86.

### Q: Нужно ли глубокое знание архитектуры x86?
A: Для базовой разработки достаточно понимания основ, но для сложных задач знание архитектуры обязательно.

### Q: Как тестировать изменения?
A: Используйте QEMU для тестирования, добавляйте unit-тесты для новых функций.

### Q: Какова политика ветвления?
A: Используйте feature-ветки для новых функций, bugfix-ветки для исправлений.

## 📞 Контакты

- **Разработчик:** @lakladon
- **Email:** developer@example.com
- **Discord:** [ссылка на Discord сервер]

## 🙏 Благодарности

Спасибо всем, кто вносит вклад в развитие проекта!

---

**Ваш вклад важен!** Каждое улучшение, каждое исправление делает LakOS лучше.