# QUICKSTART.md - Быстрый старт за 5 минут

Это руководство поможет вам быстро запустить LakOS на вашем компьютере.

## 📋 Предварительные требования

Для сборки LakOS вам понадобятся:

- **Linux/macOS** (или WSL2 на Windows)
- **GCC** с поддержкой 32-битной компиляции
- **NASM** - ассемблер
- **Limine** (bootloader + deploy utility)
- **xorriso** - для создания ISO

### Установка зависимостей

**Ubuntu/Debian:**
```bash
sudo apt update
sudo apt install build-essential nasm xorriso limine
```

**macOS:**
```bash
brew install nasm xorriso
# Limine можно установить через пакетный менеджер или собрать из исходников
```

**Windows (WSL2):**
```bash
sudo apt update
sudo apt install build-essential nasm xorriso limine
```

## 🚀 Сборка и запуск

### 1. Клонирование репозитория

```bash
git clone https://github.com/yourusername/lakos-os.git
cd lakos-os
```

### 2. Сборка системы

```bash
# Собрать все компоненты
make all

# Или по отдельности:
make kernel    # Собрать ядро
make rootfs    # Собрать файловую систему
make iso       # Создать ISO-образ
```

### 3. Запуск в эмуляторе

**QEMU (рекомендуется):**
```bash
# Запустить с 512MB RAM
qemu-system-i386 -cdrom lakos.iso -m 512M

# Запустить с отладкой
qemu-system-i386 -cdrom lakos.iso -m 512M -s -S
```

**VirtualBox:**
1. Создайте новый виртуальный компьютер
2. Выберите тип: Linux, версия: Other Linux (32-bit)
3. Укажите 512MB RAM
4. В настройках загрузки выберите lakos.iso

## 📁 Что создано

После сборки в корне проекта появятся:

- `lakos.bin` - бинарный образ ядра
- `modules.tar` - архив корневой файловой системы
- `lakos.iso` - загрузочный ISO-образ для запуска

## 🎮 Первые шаги в LakOS

После загрузки вы увидите приглашение shell:

```bash
lakos> _
```

### Базовые команды

```bash
# Просмотреть список доступных команд
lakos> help

# Просмотреть файловую систему
lakos> ls

# Перейти в другую директорию
lakos> cd bin

# Запустить программу
lakos> calc

# Запустить GUI
lakos> gui

# Выход из системы
lakos> exit
```

### Работа с файлами

```bash
# Просмотреть содержимое файла
lakos> cat test.txt

# Создать файл (через перенаправление)
lakos> echo "Hello LakOS" > hello.txt

# Просмотреть созданный файл
lakos> cat hello.txt
```

## 🔧 Типичные проблемы

### Проблема: "make: *** No rule to make target 'iso'"
**Решение:** Убедитесь, что вы находитесь в корне проекта и Makefile существует.

### Проблема: "qemu-system-i386: command not found"
**Решение:** Установите QEMU:
```bash
# Ubuntu/Debian
sudo apt install qemu-system-i386

# macOS
brew install qemu
```

### Проблема: "No bootable device"
**Решение:** Проверьте, что lakos.iso был успешно создан и имеет размер > 1MB.

## 📚 Следующие шаги

- [USER_GUIDE.md](USER_GUIDE.md) - Подробное руководство пользователя
- [BUILDING.md](BUILDING.md) - Детали процесса сборки
- [DEVELOPER_GUIDE.md](DEVELOPER_GUIDE.md) - Как внести свой вклад в разработку

## 🆘 Получение помощи

Если у вас возникли проблемы:

1. Проверьте [Issues](https://github.com/yourusername/lakos-os/issues) в репозитории
2. Создайте новый Issue с описанием проблемы
3. Приложите логи сборки и запуска

---

**Готово!** Теперь вы можете экспериментировать с LakOS и изучать основы разработки операционных систем.